# Context

Current state snapshot of `ActDim.Three.NewtonsoftJson`.

## Overview
- Standalone compatibility adapter for Newtonsoft.Json serialization of `ActDim.Three` scene graphs and primitives (`ThreeNewtonsoftSerializer`).
- Cleanly decoupled from core `ActDim.Three` engine library.

