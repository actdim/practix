# Context

Current state snapshot of `ActDim.Emitron`.

## Overview
- Roslyn C# script engine (`ScriptEngine`), template string compiler (`Interpolator`), and string interpolation extensions (`template.Interpolate(input)`).
- Standardized on collision-free `@params` parameter variable binding with custom `inputParameterName` support.
- All 37 unit tests passing in `ActDim.Emitron.Tests`.
