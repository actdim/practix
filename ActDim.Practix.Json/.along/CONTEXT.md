# Context

Current state snapshot of `ActDim.Practix.Json`.

## Overview
- Autonomous JSON serialization subsystem (`CoreJsonSerializer`).
- Expression Tree property setters, `CopyOptions` cloning, custom converters, and DI registration (`AddPractixJson()`).
- All 102 unit tests passing in `ActDim.Practix.Json.Tests`.

